-- ============================================
-- PROJECT BACKPACK - ADAPTADOR COMPLETO
-- ============================================
_G.ScriptAdapter = _G.ScriptAdapter or {}
_G.ScriptAdapter.Queue = _G.ScriptAdapter.Queue or {}
_G.ScriptAdapter.RegisteredTools = _G.ScriptAdapter.RegisteredTools or {}

local MAX_SLOTS = 1
_G.ScriptAdapter.HotbarSlots = _G.ScriptAdapter.HotbarSlots or {nil}

local Adapter = _G.ScriptAdapter
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local lp = Players.LocalPlayer
local playerGui = lp:WaitForChild("PlayerGui")

if playerGui:FindFirstChild("ProjectBackpackGui") then
    playerGui.ProjectBackpackGui:Destroy()
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ProjectBackpackGui"
screenGui.ResetOnSpawn = false

pcall(function() screenGui.Parent = CoreGui end)
if not screenGui.Parent then screenGui.Parent = playerGui end

-- BOTÃO DE ABRIR / FECHAR MOCHILA
local toggleBtn = Instance.new("ImageButton")
toggleBtn.Name = "ToggleBackpackBtn"
toggleBtn.Size = UDim2.new(0, 42, 0, 42)
toggleBtn.Position = UDim2.new(0.5, -21, 0.02, 0)
toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
toggleBtn.BackgroundTransparency = 0.2
toggleBtn.Image = "rbxassetid://73146839145612"
toggleBtn.Active = true
toggleBtn.Draggable = true
toggleBtn.Parent = screenGui

local toggleStroke = Instance.new("UIStroke")
toggleStroke.Color = Color3.fromRGB(255, 255, 255)
toggleStroke.Thickness = 1.5
toggleStroke.Parent = toggleBtn

-- PAINEL DA MOCHILA (INVENTÁRIO)
local mainPanel = Instance.new("Frame")
mainPanel.Name = "MainPanel"
mainPanel.Size = UDim2.new(0, 230, 0, 230)
mainPanel.Position = UDim2.new(0.5, -115, 0.28, 0)
mainPanel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
mainPanel.BorderSizePixel = 0
mainPanel.Visible = false
mainPanel.Active = true
mainPanel.Draggable = true
mainPanel.Parent = screenGui

Adapter.MainPanel = mainPanel

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(255, 255, 255)
mainStroke.Thickness = 2
mainStroke.Parent = mainPanel

local header = Instance.new("Frame")
header.Size = UDim2.new(1, -6, 0, 30)
header.Position = UDim2.new(0, 3, 0, 3)
header.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
header.BorderSizePixel = 0
header.Parent = mainPanel

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 1, 0)
title.BackgroundTransparency = 1
title.Text = "Project Backpack"
title.TextColor3 = Color3.fromRGB(0, 0, 0)
title.Font = Enum.Font.SpecialElite
title.TextSize = 16
title.Parent = header

local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Name = "ToolsScroll"
scrollFrame.Size = UDim2.new(1, -10, 1, -42)
scrollFrame.Position = UDim2.new(0, 5, 0, 37)
scrollFrame.BackgroundTransparency = 1
scrollFrame.BorderSizePixel = 0
scrollFrame.ScrollBarThickness = 3
scrollFrame.ScrollBarImageColor3 = Color3.fromRGB(255, 255, 255)
scrollFrame.Parent = mainPanel

local grid = Instance.new("UIGridLayout")
grid.CellSize = UDim2.new(0, 65, 0, 65)
grid.CellPadding = UDim2.new(0, 6, 0, 6)
grid.HorizontalAlignment = Enum.HorizontalAlignment.Left
grid.VerticalAlignment = Enum.VerticalAlignment.Top
grid.SortOrder = Enum.SortOrder.LayoutOrder
grid.Parent = scrollFrame

-- CONTAINER DA HOTBAR (POSICIONADO MAIS ALTO)
local containerWidth = MAX_SLOTS * 65 + (MAX_SLOTS - 1) * 6
local hotbarContainer = Instance.new("Frame")
hotbarContainer.Name = "HotbarContainer"
hotbarContainer.Size = UDim2.new(0, containerWidth, 0, 68)
hotbarContainer.Position = UDim2.new(0.5, -math.floor(containerWidth / 2), 0.78, 0)
hotbarContainer.BackgroundTransparency = 1
hotbarContainer.Parent = screenGui

local hotbarLayout = Instance.new("UIListLayout")
hotbarLayout.FillDirection = Enum.FillDirection.Horizontal
hotbarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
hotbarLayout.SortOrder = Enum.SortOrder.LayoutOrder
hotbarLayout.Padding = UDim.new(0, 6)
hotbarLayout.Parent = hotbarContainer

-- SLOTS DE HOTBAR
local hotbarSlotFrames = {}
for i = 1, MAX_SLOTS do
    local slotFrame = Instance.new("Frame")
    slotFrame.Name = "Slot_" .. i
    slotFrame.Size = UDim2.new(0, 65, 0, 65)
    slotFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    slotFrame.BackgroundTransparency = 0.5
    slotFrame.Parent = hotbarContainer

    local sStroke = Instance.new("UIStroke")
    sStroke.Color = Color3.fromRGB(255, 255, 255)
    sStroke.Thickness = 1.5
    sStroke.Parent = slotFrame

    local sCorner = Instance.new("UICorner")
    sCorner.CornerRadius = UDim.new(0, 2)
    sCorner.Parent = slotFrame

    hotbarSlotFrames[i] = slotFrame
end

-- ATUALIZADOR DE RE-RENDERIZAÇÃO DA UI
function Adapter:RefreshUI()
    for i = 1, MAX_SLOTS do
        local slotContainer = hotbarSlotFrames[i]
        for _, child in pairs(slotContainer:GetChildren()) do
            if child:IsA("GuiObject") and not child:IsA("UIStroke") and not child:IsA("UICorner") then
                child.Parent = nil
            end
        end

        local toolData = Adapter.HotbarSlots[i]
        if toolData and toolData.ButtonUI then
            toolData.ButtonUI.Size = UDim2.new(1, 0, 1, 0)
            toolData.ButtonUI.Position = UDim2.new(0, 0, 0, 0)
            toolData.ButtonUI.Parent = slotContainer
        end
    end

    for _, child in pairs(scrollFrame:GetChildren()) do
        if child:IsA("GuiObject") and not child:IsA("UIGridLayout") then
            child.Parent = nil
        end
    end

    for id, toolData in pairs(Adapter.RegisteredTools) do
        local inHotbar = false
        for i = 1, MAX_SLOTS do
            if Adapter.HotbarSlots[i] and Adapter.HotbarSlots[i].Id == id then
                inHotbar = true
                break
            end
        end

        if not inHotbar and toolData.ButtonUI then
            toolData.ButtonUI.Size = UDim2.new(0, 65, 0, 65)
            toolData.ButtonUI.Position = UDim2.new(0, 0, 0, 0)
            toolData.ButtonUI.Parent = scrollFrame
        end
    end
end

-- SISTEMA DE DRAG & DROP COM THRESHOLD DE MOVIMENTO
local function SetupDragAndDrop(toolData)
    local btn = toolData.ButtonUI
    if not btn then return end

    local isDragging = false
    local dragProxy = nil
    local dragConn = nil
    local startPos = nil
    local dragThreshold = 8

    btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            startPos = UserInputService:GetMouseLocation()

            dragConn = RunService.RenderStepped:Connect(function()
                local mPos = UserInputService:GetMouseLocation()
                
                -- Ativa o arrasto apenas se mover além do limite (evita conflito com cliques)
                if not isDragging and startPos and (mPos - startPos).Magnitude > dragThreshold then
                    isDragging = true
                    dragProxy = btn:Clone()
                    dragProxy.Size = UDim2.new(0, 65, 0, 65)
                    dragProxy.AnchorPoint = Vector2.new(0.5, 0.5)
                    dragProxy.ZIndex = 100
                    dragProxy.BackgroundTransparency = 0.2
                    dragProxy.Parent = screenGui
                end

                if isDragging and dragProxy then
                    dragProxy.Position = UDim2.new(0, mPos.X, 0, mPos.Y - 36)
                end
            end)
        end
    end)

    local function FinalizarArrasto()
        if dragConn then
            dragConn:Disconnect()
            dragConn = nil
        end

        if isDragging then
            isDragging = false
            if dragProxy then
                dragProxy:Destroy()
                dragProxy = nil
            end

            local mousePos = UserInputService:GetMouseLocation()

            local targetSlotIndex = nil
            for i = 1, MAX_SLOTS do
                local slotFrame = hotbarSlotFrames[i]
                local pos = slotFrame.AbsolutePosition
                local size = slotFrame.AbsoluteSize

                if mousePos.X >= pos.X and mousePos.X <= pos.X + size.X and
                   mousePos.Y >= pos.Y and mousePos.Y <= pos.Y + size.Y then
                    targetSlotIndex = i
                    break
                end
            end

            local currentSlotIndex = nil
            for i = 1, MAX_SLOTS do
                if Adapter.HotbarSlots[i] and Adapter.HotbarSlots[i].Id == toolData.Id then
                    currentSlotIndex = i
                    break
                end
            end

            if targetSlotIndex then
                if currentSlotIndex then
                    local itemNoAlvo = Adapter.HotbarSlots[targetSlotIndex]
                    Adapter.HotbarSlots[targetSlotIndex] = toolData
                    Adapter.HotbarSlots[currentSlotIndex] = itemNoAlvo
                else
                    Adapter.HotbarSlots[targetSlotIndex] = toolData
                end
            else
                local mainPos = mainPanel.AbsolutePosition
                local mainSize = mainPanel.AbsoluteSize
                local dentroDaMochila = (mousePos.X >= mainPos.X and mousePos.X <= mainPos.X + mainSize.X and
                                        mousePos.Y >= mainPos.Y and mousePos.Y <= mainPos.Y + mainSize.Y)

                if dentroDaMochila or not mainPanel.Visible then
                    if currentSlotIndex then
                        Adapter.HotbarSlots[currentSlotIndex] = nil
                    end
                end
            end

            Adapter:RefreshUI()
        end

        startPos = nil
    end

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            FinalizarArrasto()
        end
    end)
end

function Adapter:RegisterTool(toolData)
    Adapter.RegisteredTools[toolData.Id] = toolData
    SetupDragAndDrop(toolData)
    Adapter:RefreshUI()
end

toggleBtn.MouseButton1Click:Connect(function()
    mainPanel.Visible = not mainPanel.Visible
end)

for _, pendingTool in ipairs(Adapter.Queue) do
    Adapter:RegisterTool(pendingTool)
end
Adapter.Queue = {}

Adapter:RefreshUI()


-- ============================================
-- FLING BALL TOOL (COMPATÍVEL COM PROJECT BACKPACK)
-- ============================================
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local lp = Players.LocalPlayer
local character = lp.Character or lp.CharacterAdded:Wait()

lp.CharacterAdded:Connect(function(newChar)
    character = newChar
end)

local sethp = sethiddenproperty or set_hidden_property
if sethp then
    task.spawn(function()
        RunService.RenderStepped:Connect(function()
            pcall(function()
                sethp(lp, "SimulationRadius", 1e30)
                sethp(lp, "MaxSimulationRadius", 1e30)
            end)
        end)
    end)
end

local camera = Workspace.CurrentCamera

local function executarAtaqueLimitado(alvoPlayer)
    local alvoChar = alvoPlayer.Character
    if not alvoChar or not alvoChar:FindFirstChild("HumanoidRootPart") then return end

    local alvoHum = alvoChar.Humanoid
    local vCharacter = lp.Character
    local meuHRP = vCharacter and vCharacter:FindFirstChild("HumanoidRootPart")

    local remote = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Too1l")  

    if not lp.Backpack:FindFirstChild("SoccerBall") and not character:FindFirstChild("SoccerBall") then
        remote:InvokeServer("PickingTools", "SoccerBall")
    end  

    repeat task.wait() until lp.Backpack:FindFirstChild("SoccerBall") or character:FindFirstChild("SoccerBall")  

    local sTool = lp.Backpack:FindFirstChild("SoccerBall") or character:FindFirstChild("SoccerBall")
    sTool.Parent = character  

    local ballName = "Soccer" .. lp.Name
    local ballFolder = Workspace:WaitForChild("WorkspaceCom"):WaitForChild("001_SoccerBalls")

    local bolaFisica = nil
    repeat task.wait() until ballFolder:FindFirstChild(ballName)
    bolaFisica = ballFolder:FindFirstChild(ballName)  

    sTool.Parent = lp.Backpack  

    if bolaFisica and meuHRP then
        if sethp then
            pcall(function() sethp(bolaFisica, "NetworkOwner", lp) end)
        elseif bolaFisica.SetNetworkOwner then
            pcall(function() bolaFisica:SetNetworkOwner(lp) end)
        end

        local nc = Instance.new("NoCollisionConstraint", bolaFisica)
        nc.Part0 = bolaFisica
        nc.Part1 = meuHRP

        bolaFisica.CanCollide = true
        bolaFisica.Massless = false
        bolaFisica.CustomPhysicalProperties = PhysicalProperties.new(100, 100, 10, 100, 100)

        bolaFisica.Velocity = Vector3.new(0,0,0)
        bolaFisica.RotVelocity = Vector3.new(0,0,0)

        local forcaMassiva = Vector3.new(math.huge, math.huge, math.huge)

        local velocity = Instance.new("BodyVelocity")
        velocity.Name = "FlingPowerLinear"
        velocity.Velocity = Vector3.new(999999, 999999, 999999)
        velocity.MaxForce = forcaMassiva
        velocity.P = math.huge
        velocity.Parent = bolaFisica  

        local angularVelocity = Instance.new("BodyAngularVelocity")
        angularVelocity.Name = "FlingPowerAngular"
        angularVelocity.AngularVelocity = Vector3.new(999999, 999999, 999999)
        angularVelocity.MaxTorque = forcaMassiva
        angularVelocity.P = math.huge
        angularVelocity.Parent = bolaFisica

        local partesAlvo = {"HumanoidRootPart", "Head", "LowerTorso"}

        task.spawn(function()
            local index = 1
            local conexaoFisica

            conexaoFisica = RunService.Heartbeat:Connect(function()
                if not bolaFisica or not bolaFisica.Parent or alvoHum.Health <= 0 or not alvoChar.Parent then
                    if conexaoFisica then conexaoFisica:Disconnect() end
                    if bolaFisica then bolaFisica:Destroy() end
                    return
                end

                bolaFisica.CanCollide = true

                local nomeParte = partesAlvo[index]
                local p = alvoChar:FindFirstChild(nomeParte)

                if p then
                    local vReal = p.AssemblyLinearVelocity
                    local velocidadePlana = Vector3.new(vReal.X, 0, vReal.Z)
                    local posicaoFinal = p.Position

                    if velocidadePlana.Magnitude > 1 then
                        local direcaoMovimento = velocidadePlana.Unit
                        local studsDeCompensacao = (velocidadePlana.Magnitude <= 18) and 14.2 or 20.2
                        posicaoFinal = p.Position + (direcaoMovimento * studsDeCompensacao)
                    end

                    bolaFisica.CFrame = CFrame.new(posicaoFinal)
                    bolaFisica.Velocity = Vector3.new(999999, 999999, 999999)
                    bolaFisica.RotVelocity = Vector3.new(999999, 999999, 999999)

                    firetouchinterest(bolaFisica, p, 0)
                    firetouchinterest(bolaFisica, p, 1)
                end

                index = index + 1
                if index > #partesAlvo then index = 1 end
            end)
        end)
    end
end

-- CRIAÇÃO DO BOTÃO VISUAL DA TOOL
local actionButton = Instance.new("TextButton")
actionButton.Name = "FlingBallButton"
actionButton.Size = UDim2.new(0, 65, 0, 65)
actionButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
actionButton.BackgroundTransparency = 0
actionButton.BorderSizePixel = 0
actionButton.Text = "FLING\nBALL"
actionButton.TextColor3 = Color3.fromRGB(255, 255, 255)
actionButton.TextTransparency = 0
actionButton.Font = Enum.Font.Arcade
actionButton.TextScaled = true
actionButton.TextWrapped = true

local uiPadding = Instance.new("UIPadding")
uiPadding.PaddingTop = UDim.new(0.08, 0)
uiPadding.PaddingBottom = UDim.new(0.08, 0)
uiPadding.PaddingLeft = UDim.new(0.08, 0)
uiPadding.PaddingRight = UDim.new(0.08, 0)
uiPadding.Parent = actionButton

local uiStroke = Instance.new("UIStroke")
uiStroke.Color = Color3.fromRGB(255, 255, 255)
uiStroke.Thickness = 3
uiStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
uiStroke.LineJoinMode = Enum.LineJoinMode.Miter
uiStroke.Parent = actionButton

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(0, 2)
uiCorner.Parent = actionButton

local modoAtaqueAtivo = false

local function desativarModoAtaque()
    modoAtaqueAtivo = false
    actionButton.Text = "FLING\nBALL"
    actionButton.BackgroundTransparency = 0
    actionButton.TextTransparency = 0
end

actionButton.MouseButton1Click:Connect(function()
    if _G.ScriptAdapter and _G.ScriptAdapter.MainPanel and _G.ScriptAdapter.MainPanel.Visible then
        return
    end

    modoAtaqueAtivo = not modoAtaqueAtivo
    
    if modoAtaqueAtivo then
        actionButton.BackgroundTransparency = 0.45
        actionButton.TextTransparency = 0.2
    else
        desativarModoAtaque()
    end
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed or not modoAtaqueAtivo then return end

    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        local pos = input.Position
        local unitRay = camera:ScreenPointToRay(pos.X, pos.Y)
        
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
        raycastParams.FilterDescendantsInstances = {character}

        local raycastResult = Workspace:Raycast(unitRay.Origin, unitRay.Direction * 50000, raycastParams)

        if raycastResult and raycastResult.Instance then
            local hitPart = raycastResult.Instance
            local model = hitPart:FindFirstAncestorOfClass("Model")

            if model and model:FindFirstChild("Humanoid") then
                local tPlayer = Players:GetPlayerFromCharacter(model)

                if tPlayer and tPlayer ~= lp then
                    actionButton.Text = "LANÇAR\nBALL"
                    executarAtaqueLimitado(tPlayer)
                    
                    task.delay(0.5, function()
                        desativarModoAtaque()
                    end)
                end
            end
        end
    end
end)

-- REGISTRO DA TOOL NO ADAPTADOR
_G.ScriptAdapter = _G.ScriptAdapter or {}
_G.ScriptAdapter.Queue = _G.ScriptAdapter.Queue or {}

local toolData = {
    Id = "FlingBall_Tool",
    ButtonUI = actionButton,
    OnEquip = function()
        modoAtaqueAtivo = true
    end,
    OnUnequip = function()
        desativarModoAtaque()
    end
}

if _G.ScriptAdapter and _G.ScriptAdapter.RegisterTool then
    _G.ScriptAdapter:RegisterTool(toolData)
else
    table.insert(_G.ScriptAdapter.Queue, toolData)
end